# Copier ce fichier en .env et renseigner les valeurs réelles.
# Ne jamais committer .env (voir .gitignore).

# Clé API du fournisseur LLM utilisé par les agents
# ANTHROPIC_API_KEY=

# memory-agent — moteur de base de données (SQLAlchemy). Par défaut : SQLite dans data/.
# Voir docs/adr/0001-stack-persistence-memory-agent.md
# DATABASE_URL=sqlite:///data/memory-agent.db

# Configuration à compléter au fur et à mesure de l'implémentation des agents
