# config

Configuration du projet : variables d'environnement, paramètres par agent.

Convention : `.env.example` documente les variables attendues ; `.env` (réel, avec secrets) reste hors contrôle de version.
